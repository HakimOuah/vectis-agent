# User-owned Monid key. Never paste it in chat or commit it.
MONID_API_KEY=
# Lifetime conservative commitment cap for this data directory, including prior missions.
VECTIS_MAX_USD=2
# Optional: absolute path to a private local directory, one MCP process per directory.
# VECTIS_DATA_DIR=/absolute/private/path
# No OpenAI or Anthropic key is consumed by this package.
# Configure your model subscription or API key in your MCP host assistant.
